# 8. Benchmark Methodology & Results

**Status: not yet written — blocked on T070-T074.**

Scope for this section: the benchmark framework's methodology (warm-up
iterations, per-repetition DB writes, see `Benchmark.java`/`DatabaseBenchmark.java`),
then the actual Week 4 experiment results once they exist:

- T070 (hash table load factor vs. collisions)
- T071 (BST vs. balanced tree on sorted input)
- T072 (triage-priority vs. first-come-first-served outcomes)
- T073 (graph algorithms vs. graph size/density)

[../../results/](../../results/) is currently empty (`.gitkeep` only) — this
section cannot be written until those experiments actually run and produce
CSVs/graphs. Do not fill this in with placeholder or estimated numbers.
