# 5. Data Structures Implemented

**Status: not yet written (T075).**

Scope for this section: one subsection per structure, each briefly
describing the invariant it maintains and why that structure was the right
choice for its use case in this project (not a generic textbook description).

- **Linear** (`linear/`): dynamic array, doubly linked list, array stack,
  array deque, circular queue, binary heap priority queue
- **Trees** (`trees/`): binary search tree, red-black tree (and the
  BST-vs-RB height comparison), B-tree, chained hash table, hash set
- **Graphs** (`graphs/`): adjacency list/matrix, disjoint set

Each subsection should cite its own test file's normal/boundary/invalid
coverage as evidence, not just describe the structure in the abstract.
